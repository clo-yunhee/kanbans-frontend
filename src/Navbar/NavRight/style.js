import styled from 'styled-components';

export const Container = styled.div`
    position: relative;
    right: 0;
    flex-grow: 1;

    display: flex;
    flex-direction: row;
    justify-content: end;
`;
