import styled from 'styled-components';

export const Container = styled.div`
    position: relative;
    left: 0;
    flex-grow: 1;

    display: flex;
    flex-direction: row;
    justify-content: flex-start;
`;
