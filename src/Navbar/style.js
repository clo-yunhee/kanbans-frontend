import styled from 'styled-components';

export const NavContainer = styled.nav`
    position: relative;
    z-index: 1;

    width: 100vw;
    height: 50px;

    font-size: 2rem;

    background-color: #512da8;

    display: flex;
    flex-direction: row;
`;
